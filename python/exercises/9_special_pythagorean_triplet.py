from typing import List, Tuple
import math
import itertools
from functools import reduce

# We need to find a pythagorean triplet (three numbers
# a, b, and c for which a^2 + b^2 = c^2 holds true)
# where c = 1000, there is only exactly one such
# triplet. We, then, need to return the product
# of a*b*c.


class Solution:

    def solution(self, c: int) -> int:
        pass


# Boilerplate driver code
input = 1000
print(Solution().solution(input))
